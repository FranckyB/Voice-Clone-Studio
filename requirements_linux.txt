# Voice Clone Studio - Runtime Requirements
# DramaBox-only build: core app + MMAudio + ASR + shared utilities.
# Legacy engines removed: Qwen3-TTS, LuxTTS, Chatterbox, Fish Speech.

# Core audio/model runtime dependencies (all platforms)
numpy==1.26.4
librosa
torchaudio
soundfile
mutagen
sox
einops
huggingface_hub
onnxruntime
onnxruntime-gpu; sys_platform != 'darwin'

# Web UI and utilities
gradio>=6.7.0,<6.8.0
gradio_filelister @ file:wheel/gradio_filelister-0.4.0-py3-none-any.whl
diffusers
markdown
tensorboard

# ASR dependencies
qwen-asr

# MMAudio Sound Effects dependencies (torchvision installed with PyTorch in setup script)
open_clip_torch
torchdiffeq
timm
colorlog
omegaconf
av>=14.0.1

ninja

# 4-bit quantization support (used by DramaBox/Gemma paths)
bitsandbytes

# DramaBox finetune/runtime shared deps
accelerate>=1.11.0
safetensors>=0.7.0
sentencepiece>=0.2.0
PyYAML>=6.0
pydantic==2.10.6
peft>0.19.0